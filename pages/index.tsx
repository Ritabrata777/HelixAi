// This is a Next.js page that will use the App component
// The App component handles all routing internally with React Router
// The actual rendering happens in _app.tsx, this is just a placeholder
export default function Home() {
  return <div style={{ display: 'none' }} />; // App component handles everything via _app.tsx
}

